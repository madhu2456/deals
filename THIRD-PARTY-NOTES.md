# Third-party notices

This repository contains project code plus third-party components. Project code
is the owner's: **private / all rights reserved unless otherwise noted.
© Madhu Dadi — [madhudadi.in](https://madhudadi.in)** (see README "License").

Third-party components and their licenses:

| Component | License | Notes |
|-----------|---------|-------|
| `lucide-react` (icons) | ISC | Icon library used in the UI; see `node_modules/lucide-react/LICENSE` |
| Next.js / Vercel starter assets (`public/next.svg`, `public/vercel.svg`, `public/file.svg`, `public/globe.svg`, `public/window.svg`, `app/favicon.ico`, `app/icon.png`, `app/apple-icon.png`) | MIT (Vercel template) | Assets shipped by the `create-next-app` starter template |
| `shadcn/ui` components (`components/ui/*`) | MIT | UI primitives; see `components.json` and the shadcn/ui license |
| Other npm dependencies | See `package.json` + `pnpm-lock.yaml` | Each package's license lives in its own `LICENSE`/`LICENSE.md` in `node_modules` |

This file is a brief inventory (F251), not a substitute for the license texts
shipped inside each dependency.